import { keyframes, style } from '@vanilla-extract/css'

export const qACard = style({
  width: '100%',
  height: 'auto',
  aspectRatio: '40 / 21',
  margin: '0 auto',
})

export const qACardImage = style({
  width: '100%',
  height: '100%',
})
